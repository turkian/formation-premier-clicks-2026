## 1. Content edit

- [ ] 1.1 In `src/src/content/lecons/1.yaml`, shorten the `corps` field of the
      `profondeur-de-champ` bloc's `ouverture` écran to the bare question, removing the
      sentence naming aperture and ranking it against the other levers.
- [ ] 1.2 Re-read `ce-qui-change-l-epaisseur`, `le-facteur-invisible`, and `les-quatre-leviers`
      (the three later écrans whose reveal this opener was spoiling) to confirm each still reads
      as a discovery once the opener no longer previews it.

## 2. Verification

- [ ] 2.1 Confirm the `corps` field is still valid against the `ecranRepere` schema (plain
      string, no structural change).
