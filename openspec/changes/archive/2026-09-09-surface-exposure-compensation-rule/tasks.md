## 1. Content edit

- [x] 1.1 In `src/src/content/lecons/1.yaml`, écran `configurez-maintenant` (bloc `repartir`):
      append the exposure-compensation operating rule ("si votre photo est trop sombre, tournez
      vers le `+`. Trop claire, vers le `−`. C'est tout, et ça marche. On verra pourquoi à la
      séance 2.") to the fourth `geste` string.
- [x] 1.2 Trim the écran's `aparte` down to the handout pointer only ("les mots changent d'une
      marque à l'autre — sections 1 et 4").

## 2. Verification

- [x] 2.1 Confirm the fourth `geste` still reads at a glance from the back of the room (it should
      stay a short imperative plus the rule, not grow into a paragraph).
