## 1. Layout

- [x] 1.1 In `src/src/styles/carrefour.css`, change `.liste-simple`'s `grid-template-columns` from
      `repeat(auto-fit, minmax(min(16rem, 100%), 1fr))` to a fixed single column (`1fr`).

## 2. Verification

- [x] 2.1 Open a lesson summary (`/lecons/<n>`) at a phone width and confirm each block's screens
      still stack in one column.
- [x] 2.2 Open the same page at a desktop/projector width and confirm each block's screens now
      stack in one column instead of reflowing into multiple columns.
